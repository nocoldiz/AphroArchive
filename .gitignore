# ── Dependencies ─────────────────────────────
node_modules/

# ── Python ───────────────────────────────────
__pycache__/
*.pyc

# ── Media / user data ────────────────────────
videos
audio
books
photos
pages
models
settings
process
video_downloads/
.AphroArchive-thumbs
.AphroArchive-actor-photos

# ── Media file types (images / video / audio) ─
*.jpg
*.jpeg
*.png
*.gif
*.webp
*.bmp
*.tif
*.tiff
*.avif
*.heic
*.svg
*.ico
*.mp4
*.mkv
*.webm
*.avi
*.mov
*.wmv
*.flv
*.m4v
*.mpg
*.mpeg
# (*.ts omitted — clashes with TypeScript sources)
*.mp3
*.wav
*.flac
*.ogg
*.oga
*.opus
*.m4a
*.aac
*.wma

# ── Caches ───────────────────────────────────
cache/
links_cache.json
paths.json
.aider.tags.cache.v4/cache.db
.aider.tags.cache.v4/cache.db-shm
.aider.tags.cache.v4/cache.db-wal

# ── Databases (whole db/ folder; presets/ lives in root and is tracked) ──
db/
*.db
*.db-shm
*.db-wal

# ── Build output ─────────────────────────────
build/
dist/public/*

# ── Bulk downloader ──────────────────────────
bulkdownloader/links_downloaded.txt
bulkdownloader/links_failed.txt
bulkdownloader/links_to_download.txt
# Login cookies contain auth tokens — never commit them
bulkdownloader/cookies.txt
bulkdownloader/x_cookies.txt
cookies.txt
# Runtime/local state — never commit (queue db, build zip)
bulkdownloader/queue_db.json
bulkdownloader/db.json
bulkdownloader/db.json.tmp
bulkdownloader/*.zip
# Persistent X.com browser profile — contains the logged-in session, never commit
bulkdownloader/x_browser_profile/
# Playwright / built-in browser engine + downloaded Chromium
bulkdownloader/ms-playwright/
ms-playwright/
.playwright/
# Test + tooling caches
.pytest_cache/
bulkdownloader/tests/__pycache__/

# ── Binaries ─────────────────────────────────
yt-dlp.exe

# ── Local fast build script ──────────────────
build_fast.bat
bulkdownloader/link_failed.txt
dist/links_downloaded.txt
dist/links_to_download.txt
dist

