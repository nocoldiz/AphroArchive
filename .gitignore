# ── Dependencies ─────────────────────────────
node_modules/

# ── Python ───────────────────────────────────
__pycache__/
*.pyc

# ── Media / user data ────────────────────────
videos
audio
books
photos
pages
models
settings
process
video_downloads/
.AphroArchive-thumbs
.AphroArchive-actor-photos
paths.json

# ── Media file types (images / video / audio) ─
*.jpg
*.jpeg
*.png
*.gif
*.webp
*.bmp
*.tif
*.tiff
*.avif
*.heic
*.svg
*.ico
*.mp4
*.mkv
*.webm
*.avi
*.mov
*.wmv
*.flv
*.m4v
*.mpg
*.mpeg
# (*.ts omitted — clashes with TypeScript sources)
*.mp3
*.wav
*.flac
*.ogg
*.oga
*.opus
*.m4a
*.aac
*.wma

# ── Caches ───────────────────────────────────
cache/
links_cache.json
paths.json
.aider.tags.cache.v4/cache.db
.aider.tags.cache.v4/cache.db-shm
.aider.tags.cache.v4/cache.db-wal

# ── Databases (whole db/ folder; presets/ lives in root and is tracked) ──
db/
*.db
*.db-shm
*.db-wal

# ── Build output ─────────────────────────────
build/
dist/public/*

# ── Bulk downloader (utils/bulkdownloader.py runtime files) ──
utils/links_downloaded.txt
utils/links_failed.txt
utils/links_to_download.txt
utils/link_failed.txt
# Login cookies contain auth tokens — never commit them
utils/cookies.txt
utils/x_cookies.txt
cookies.txt
# Runtime/local state — never commit (queue db, build zip)
utils/queue_db.json
utils/db.json
utils/db.json.tmp
utils/*.zip
# Persistent X.com browser profile — contains the logged-in session, never commit
utils/x_browser_profile/
# Playwright / built-in browser engine + downloaded Chromium
utils/ms-playwright/
ms-playwright/
.playwright/
# Test + tooling caches
.pytest_cache/

# ── Binaries ─────────────────────────────────
yt-dlp.exe

# ── Local fast build script ──────────────────
build_fast.bat
dist/links_downloaded.txt
dist/links_to_download.txt
dist

.aider*
GEMINI_API_KEY=AIzaSyCefHwYPxvo3HjFibd80aZPGdcpSxP5_kY
GEMINI_API_KEY=AIzaSyAnbDYetpqScam7btTaQ0SRujp3xGL9k4k
